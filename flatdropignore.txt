# .flatdropignore do repositorio FlatDrop (entregue como flatdropignore.txt).
# Controla o que a ferramenta manda ao mount do Projeto do Claude. Versionar no Git.
#
# Requer a spec0019 aplicada para (a) este arquivo chegar ao mount e (b) o nome .txt
# valer. Antes disso, renomeie para .flatdropignore.
#
# meta/specs/ e logs/ sao versionados, mas nao precisam ir ao mount toda vez —
# sao lidos sob demanda. Quando a conversa precisar de um spec/log especifico, suba
# o arquivo OU abra uma brecha temporaria aqui, por exemplo:
#   !meta/specs/260711-spec0019-flatdropignore-mount-and-aliases.md
meta/specs/
logs/

# Opcional (decida): nao mandar ao mount o .bat que so serve para flatdropar o projeto.
# Deixe comentado se quiser continuar analisando o .bat no Projeto.
# *.bat
