# spec0015 — corte da release 0.3.1 (`root_in_name` + fix de ordem)

**Tipo:** release/docs · **Alvos:** `flatdrop/__init__.py`, `meta/CHANGELOG.md`
**Autor:** chat · **Aplicador:** Claude Code
**Depende de:** spec0013 e spec0014 aplicadas (commits do `root_in_name` e do fix de
ordem já no `main`).

## Objetivo

Cortar a **0.3.1** (patch). Motivo do descompasso que esta spec resolve: a 0.3.0 foi
datada (spec0012) **antes** de a spec0013 entrar, então o `root_in_name` está no
código com `__version__ = "0.3.0"` mas **não** consta no `### Adicionado` da
`[0.3.0]` do CHANGELOG. Esta spec: (1) bumpa a versão para `0.3.1`; (2) registra em
seção própria o `root_in_name` (spec0013) e o ajuste de ordem (spec0014); (3) NÃO
reescreve a `[0.3.0]` já publicada.

Sem mudança de comportamento de código — é corte de release. Nenhum teste novo.

## Por que 0.3.1 (patch) e não 0.4.0

O `root_in_name` é uma adição pequena e opt-in (uma flag, desligada por padrão), e
grande parte do trabalho da sessão foi corrigir a própria ordem do recurso antes de
qualquer uso real. Tratar como patch de acabamento da 0.3.0 é defensável e mantém o
histórico limpo. Se preferir MINOR (0.4.0) por ser recurso novo de usuário, é só
trocar o número nas duas edições — a estrutura da spec é idêntica. **Decisão adotada
aqui: 0.3.1.** (Ver nota no fim se quiser 0.4.0.)

## Contexto / verificação (estado no mount)

- `flatdrop/__init__.py`: `__version__ = "0.3.0"`.
- CHANGELOG: `[Não lançado]` está **vazia** (placeholder "Sem mudanças ainda…"),
  seguida de `## [0.3.0] — 2026-07-04`. O `_TREE.md` já está na 0.3.0 (não repetir).
- Código: `_plan_names` já traz `dir_parts = (*reversed(dir_parts), root_prefix)`
  (ordem final correta), `root_in_name` no `ScanConfig`, `--root-in-name` na CLI,
  checkbox na GUI serializada no `.bat`. 41 testes verdes (commit `5acf16f`).

---

## EDIÇÃO 1 — `flatdrop/__init__.py`: bump 0.3.0 → 0.3.1

**Âncora:**
```
__version__ = "0.3.0"
```
**Ação:** SUBSTITUIR por:
```
__version__ = "0.3.1"
```

## EDIÇÃO 2 — `meta/CHANGELOG.md`: nova seção `[0.3.1]` entre a `[Não lançado]` e a `[0.3.0]`

Substitui o corpo placeholder da `[Não lançado]` por uma `[Não lançado]` limpa
seguida da seção datada `[0.3.1]`. **Use a data real da aplicação** (o Code troca
`AAAA-MM-DD` pela data do dia; se aplicar hoje, `2026-07-05`).

**Âncora (deve bater exatamente — se não bater, PARE e reporte):**
```
## [Não lançado]

_Sem mudanças ainda. Itens de produto em aberto: persistência/recentes (Fase 2-C),
ignores de pasta editáveis na GUI (Fase 2-D), fullpath incluindo a pasta-raiz e
multi-raiz na GUI, UI-2/UI-3._

## [0.3.0] — 2026-07-04
```
**Ação:** SUBSTITUIR por:
```
## [Não lançado]

_Sem mudanças ainda. Itens de produto em aberto: persistência/recentes (Fase 2-C),
ignores de pasta editáveis na GUI (Fase 2-D), multi-raiz na GUI, formato de nome
"caminho escrito" (raiz→pastas→stem), UI-2/UI-3._

## [0.3.1] — AAAA-MM-DD

Acabamento do modo fullpath: opção de incluir o nome da pasta-raiz no nome de cada
arquivo. Recurso pequeno e opt-in que entrou logo após o corte da 0.3.0; a mesma
sessão ajustou a ordem do sufixo antes de qualquer uso real. Suíte de 41 testes verde.

### Adicionado
- **`root_in_name` — pasta-raiz no nome (modo fullpath)** (spec0013): flag opcional
  (desligada por padrão) que, no modo `fullpath` e em **fonte única**, inclui o nome
  da pasta-raiz do projeto no nome de cada arquivo — inclusive os da raiz
  (`README.md` → `README__meuapp.md`). A injeção acontece só no **nome planejado**
  (via `root_prefix` em `_plan_names`); o `rel` de exibição do `_MANIFEST.md` e do
  `_TREE.md` permanece o caminho real. Ignorada com aviso fora do `fullpath` e em
  multi-fonte (lá o caminho já parte da raiz comum). Exposta como `--root-in-name`
  na CLI e como checkbox na GUI (serializada no `.bat`, FIX-004). O limite de nome do
  Windows segue protegido pelo truncamento com hash já existente.

### Corrigido
- **Ordem do sufixo do `root_in_name`** (spec0014): o formato passou a ser
  **stem + caminho da pasta mais interna à mais externa + nome da pasta-raiz por
  último** (ex.: `app/routes/page.tsx` sob `meuapp` → `page__routes__app__meuapp.tsx`),
  em vez da raiz logo após o stem que a primeira implementação produzia. Ajuste de
  uma linha em `_plan_names` (`(*reversed(dir_parts), root_prefix)`), sem tocar
  `_compose`; o `fullpath` sem a flag permanece idêntico.

## [0.3.0] — 2026-07-04
```

> Só o cabeçalho/placeholder da `[Não lançado]` muda; a seção `[0.3.0]` e todo o
> resto do arquivo permanecem intactos. A nova `[0.3.1]` entra ACIMA da `[0.3.0]`.
> Não mover nem reescrever nada da `[0.3.0]`.

---

## Sinais de teste / regressão

- `python -c "import flatdrop; print(flatdrop.__version__)"` → `0.3.1`.
- `python -m pytest -q` → 41 verdes (nada mudou no código; é sanidade).
- Conferir no CHANGELOG a ordem das seções, de cima para baixo: `[Não lançado]`
  (vazia) → `[0.3.1] — <data>` (com Adicionado + Corrigido) → `[0.3.0] — 2026-07-04`
  (intacta) → `[0.2.0] — 2026-06-14`. Nenhum item perdido; o `_TREE.md` continua só
  na 0.3.0 (não duplicado na 0.3.1).

## Após aplicar (Code)

Atualizar STATUS (versão → 0.3.1; resolver a nota "descompasso de versão"; mover o
`root_in_name`/ordem para "o que funciona") e ROADMAP (a "Release pendente 0.3.1" vira
concluída) nas edições append-only de praxe. Commit:

```
git add flatdrop/__init__.py meta/CHANGELOG.md meta/STATUS.md meta/ROADMAP.md
git commit -m "chore(release): corta 0.3.1" -m "Bump de 0.3.0 para 0.3.1. Registra no CHANGELOG o root_in_name (spec0013, pasta-raiz no nome no modo fullpath) e o fix de ordem (spec0014, stem + caminho invertido + raiz no fim), que entraram apos o corte da 0.3.0. Sem mudanca de comportamento. Nova secao Nao lancado limpa."
```

## Nota — se preferir 0.4.0 em vez de 0.3.1

Trocar `0.3.1` por `0.4.0` na Edição 1 e no cabeçalho da Edição 2, e ajustar a
mensagem de commit. O conteúdo das listas Adicionado/Corrigido é o mesmo. MINOR faz
sentido se você encara `root_in_name` como recurso novo de usuário; PATCH faz sentido
como acabamento da 0.3.0. Não há resposta errada — é política de versionamento sua.
