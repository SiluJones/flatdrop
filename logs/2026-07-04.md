# Log — 2026-07-04

## Objetivo da sessão
Dois blocos no mesmo dia: (manhã) autorar a spec do `_TREE.md`; (tarde) verificar a
aplicação da spec0011 pelo Code, conferir o `_TREE.md` gerado, cortar a 0.3.0 e
autorar a próxima tarefa de produto (fullpath com pasta-raiz).

## O que foi feito
- **Manhã — spec0011 autorada** (`_TREE.md`): árvore da origem na saída, pastas
  ignoradas colapsadas em uma linha sem recursão; desligada por padrão; detalhe dos
  pulados via `tree_skipped` summary/full; `_scan` estendido para a lista completa
  de pulados. Explicado ao usuário o `.flatdropignore` (3 camadas de filtro).
- **Tarde — verificação da aplicação (Code, commit `304440a`):** relidas as 3 notas
  `.txt` do mount (0820, 0714, 1528). Conferidas as edições no `config.py`/`core.py`
  (TREE_NAME/SIGNATURE, denylist com `_manifest.md`+`_tree.md`, `write_tree`,
  `skipped_items`). Rodado `write_tree` de verdade no sandbox (como pacote) — gera
  árvore, colapsa `__pycache__/` em uma linha, sem interior. **27 -> 35 testes**
  (Code reportou; sandbox sem pytest, validação por execução real + leitura).
- **`_TREE.md` do projeto conferido no mount:** correto — assinatura na 1a linha,
  `.git/`/`.pytest_cache/`/`__pycache__/` colapsados com `[ignorada: embutido]`,
  `README.md [renomeado: README__meta.md]` marcado. `Pulados: 0` (projeto sem
  `.flatdropignore` nem arquivos pulados por tipo — esperado).
- **spec0012 autorada** (corte da 0.3.0): bump `__init__` 0.2.0->0.3.0, acrescenta o
  `_TREE.md` ao CHANGELOG, data `[0.3.0]`, abre nova `[Não lançado]`.
- **spec0013 autorada** (fullpath com pasta-raiz): flag `root_in_name`; raiz só no
  nome (via `root_prefix` em `_plan_names`), não no `rel` do manifesto/tree;
  ignorada com aviso fora do fullpath e em multi-fonte; limite do Windows protegido
  pelo truncamento com hash.
- Respondida a nota 0714 (o usuário não sabia usar o `.flatdropignore`): explicado o
  uso manual atual e refinada a ideia dele de KCM (Claude lê `_TREE.md` -> dita
  `.flatdropignore` versionado). Capturada no IDEAS.
- Atualizados STATUS, IDEAS, ROADMAP; log consolidado do dia.

## Decisões
- Nenhuma DEC/FIX nova no DECISIONS. As decisões de design da sessão ficaram
  registradas nas próprias specs (0011: `tree_skipped`, retorno do `_scan`; 0013:
  raiz no nome e não no `rel`, escopo fonte-única). Quando o Code aplicar a 0013,
  avaliar se `root_in_name`/`root_prefix` merecem uma DEC curta.

## Bugs / problemas
- Nenhum. A aplicação da spec0011 saiu fiel; os 8 testes novos superam a estimativa
  (~6). Deixado para o Code, na spec0011, o ponto do `_MANIFEST.md` na denylist — o
  Code confirmou que não estava lá e adicionou (`_manifest.md`+`_tree.md`).

## Aprendizados / armadilhas
- **Injetar a raiz no `rel` contaminaria o manifesto e o `_TREE.md`** (coluna de
  origem inflada). Solução: a raiz entra só no nome planejado, via parâmetro em
  `_plan_names`; o `rel` de exibição permanece real. Registrado na spec0013.
- **`.flatdropignore` para liberar o que o git esconde:** arquivos fora do Git não
  aparecem no mount; o fluxo KCM serve sobretudo para `!padrão` (reinclusão), não
  para excluir mais.
- Validar código no sandbox por **execução real** quando o pytest não está
  disponível (rodar a função-alvo como pacote) dá confiança antes de aceitar a
  aplicação do Code.

## Onde parei
spec0011 aplicada/verificada; specs 0012 e 0013 autoradas e entregues; STATUS,
IDEAS e ROADMAP atualizados. Nenhum código de produto tocado pelo chat (raia do Code).

## Próximos passos
- Aplicar no Claude Code, nesta ordem: `/apply-spec 260704-spec0012-release-0.3.0.md`
  (corta a 0.3.0), depois `/apply-spec 260704-spec0013-fullpath-root.md`.
- Rodar `python -m pytest -q` (0012: 35 verdes; 0013: ~41). Conferir `git diff`.
- Commitar com os blocos prontos de cada spec.
- Depois: considerar rascunhar o trecho de KCM (ler `_TREE.md` -> gerar
  `.flatdropignore`) e documentar o `.flatdropignore` no README de usuário.

---

## Adendo — sessão da noite (verificação da 0013 + spec0014)

### O que foi feito
- Relidas as notas do mount: **spec0012 aplicada** (nota 2137, commit `e3dc08b`) e
  **spec0013 aplicada** (nota 2144), 35→41 testes verdes.
- **Alerta do Code (nota 2144) investigado a fundo:** o `root_in_name` gerou
  `page__meuapp__app__routes.tsx` (raiz no MEIO). Rodei o código real no sandbox e
  comparei com o fullpath comum (`page__app__routes`): confirmei que o Code estava
  certo e o **exemplo da minha spec0013 é que estava errado** (ordem invertida, raiz
  no lugar errado). Nenhum bug de código — a implementação era coerente com a
  convenção; o que faltava era decidir o formato.
- Usuário escolheu o formato "modo real": stem na frente, caminho invertido, raiz no
  fim (`page__routes__app__meuapp.tsx`).
- **spec0014 autorada** e validada por execução real: muda 1 linha em `_plan_names`
  (`(*reversed(dir_parts), root_prefix)`), sem tocar `_compose`. Produz exatamente o
  alvo; `rel` de exibição segue real (manifesto/tree intactos).
- Formato "caminho escrito" (`meuapp__app__routes__page.tsx`) capturado no IDEAS como
  seletor de formato futuro (útil ao usuário, não ao Claude).

### Decisões / pontos de atenção
- **Descompasso de versão detectado:** a 0.3.0 foi cortada ANTES da spec0013, então o
  `root_in_name` está no código 0.3.0 mas não no CHANGELOG da `[0.3.0]`. Encaminhado:
  root_in_name + fix da 0014 saem numa **0.3.1** (registrado no STATUS e no ROADMAP).
  NÃO reescrever a `[0.3.0]` já datada.
- Sem DEC/FIX nova (ajuste de formato, não decisão de arquitetura nem bug grave).

### Próximos passos
- Aplicar `/apply-spec 260704-spec0014-root-order.md`; `pytest` (~41 verdes).
- Depois: autorar a spec de release **0.3.1** (bump patch + registrar root_in_name e o
  fix no CHANGELOG). Em algum momento, o seletor de formato "caminho escrito" se o
  usuário quiser.
