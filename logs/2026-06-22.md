# Log — 2026-06-22

## Objetivo da sessão
Integrar a atualização do kit (CEREBRO.md + modo Claude Code), adicionar tipos de arquivo que
faltavam, varrer todos os `.txt` pendentes (dizer quais já foram extraídos), e planejar as próximas
features (gerador de `.bat`, opções faltantes na GUI, `.flatdropignore`).

## O que foi feito
- **Comparação CEREBRO.md × CLAUDE.md (antigo).** CEREBRO é superconjunto EXATO: princípios 1-19,
  higiene, privacidade e transferência idênticos palavra por palavra; só acrescenta a nota de adaptar
  instruções, a seção "raias chat↔Code" e o apêndice de arranque. Nada se perdeu — sem merge.
- **Modo Claude Code adotado (DEC-012).** Criados os arquivos de arranque: `CLAUDE.md` (raiz),
  `.claude/settings.json` (com pytest), `.claude/commands/apply-spec.md` e `wrap.md`. Adotado o método
  "doc por spec".
- **Pesquisa dos tipos aceitos pelo Projeto do Claude** (PDF/DOCX/XLSX + ODT/RTF/EPUB). Autorada a
  `spec-0001` para acrescentar à allowlist: Godot (`gd`, `uid`, `tscn`, `tres`, `gdshader`, `godot`,
  `import`), os documentos aceitos e um conjunto curado de linguagens/configs. Imagens/áudio/vídeo fora.
- **Lidos todos os `.txt`.** Tabela de extração feita (todos viram arquiváveis). Ideias novas capturadas:
  `.flatdropignore`, seleção de tipo na GUI, liberação do `.gitignore` na GUI, tratamento de ignorados
  no `_TREE`. Registradas via `spec-0002` (+ Feedback para o Kit).
- **Specs de doc (spec-0002)** para DECISIONS (DEC-012, DEC-013), IDEAS, ROADMAP e STATUS — append-only.

## Decisões
- DEC-012: desenvolvimento em duas raias (chat autora, Code implementa).
- DEC-013: expandir a allowlist de defaults (sem virar pega-tudo; ajuste fino futuro via `.flatdropignore`).

## Bugs / problemas
- **Consoles 260615/260617:** erros do CMD (`'FlatDrop'`/`'m'`/`'Use'`) antes do `CONCLUÍDO`. O Python
  roda certo (multi-fonte OK, 86 arquivos). Causa provável no conteúdo do `.bat` — preciso do arquivo
  real do usuário para cravar a causa raiz. Reforça o valor do gerador de `.bat`.

## Aprendizados / armadilhas
- O Projeto do Claude aceita binários de documento (PDF/DOCX/XLSX/ODT/RTF/EPUB); a estimativa de tokens
  (`bytes/4`) não vale para eles.
- `.gd.uid` parte como ext `uid` no `split_name`; basta adicionar `uid`.
- Inflar o config para sempre não escala — `.flatdropignore` (com negação para liberar) é o caminho
  para o ajuste fino por projeto.
- Padrão profissional para árvores: NÃO recursar em pastas ignoradas — colapsar em uma linha (evita
  inchaço, ex.: node_modules).

## Onde parei
Specs prontas (spec-0001 código, spec-0002 docs) para aplicar no Claude Code; arranque do Code criado.
As features maiores (gerador de `.bat`, seleção de tipo na GUI, liberação do `.gitignore`,
`.flatdropignore`) estão em design, aguardando algumas decisões do usuário antes de autorar as specs.

## Próximos passos
- Usuário aplica `spec-0001` e `spec-0002` no Claude Code (e cria os arranque), commita.
- Usuário cola o conteúdo real de um `.bat` para cravar a causa dos erros do CMD.
- Decididas as perguntas, autorar specs para: gerador de `.bat` na GUI, seleção de tipo na GUI,
  liberação do `.gitignore`, `.flatdropignore` + `.gitignore` aninhado, `_TREE.md`.
