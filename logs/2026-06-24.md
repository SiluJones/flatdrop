# Log — 2026-06-24

> Substitui versoes anteriores deste arquivo (cobre o dia inteiro).

## Objetivo da sessao
Diagnosticar o erro dos `.bat` no CMD; autorar as specs das features confirmadas; reagir ao teste do
usuario (GUI + `.bat` gerado); desenhar e (aprovado) implementar a UI-1; e responder sobre um launcher.

## O que foi feito
- **FIX-003:** `.bat` com travessao/acentos + `chcp 65001` desalinha o CMD. Os 5 `.bat` do cinzeiro
  reentregues em ASCII (sem `--add-ext`, redundante apos spec-0001).
- **spec-0003 (GUI: filtros + gerador de `.bat`)** e **spec-0004 (docs)** APLICADAS (commit e56aa81; 26/26).
- **Teste do usuario:** o `.bat` gerado rodou limpo (ASCII OK) e fez multi-fonte (89 arquivos, 2 fontes).
  O AVISO de `bat/` pulada e o FIX-001 (nada perdido). Mas o "Executar" na GUI so fez a coleta normal -> FIX-004.
- **FIX-004 (spec-0005):** o toggle multi-fonte so estava ligado ao gerador de `.bat`, nao a execucao ao
  vivo. Helper `_sources` + `make_plan_sources` em `_on_preview`/`_on_execute`. Tambem: "Procurar..." do
  multi-fonte abre na pasta-pai da raiz; janela abre maximizada.
- **spec-0006 (docs):** FIX-004 + estado + ideias da UI.
- **Design da UI aprovado** pelo usuario -> **spec-0007 (UI-1)**: caixa de extensoes e campos so/exceto
  trocados por um botao "Escolher tipos..." que abre um MODAL (checklist categorizado + busca + marcar/
  limpar por grupo + adicionar custom). Tela principal compacta (so um resumo). O `.bat` reproduz a
  selecao com `--add-ext` (adicoes) + `--exclude-ext` (remocoes). "Gerar .bat..." abre na pasta-pai.
- **Launcher `flatdrop-ui.bat`** entregue: abre a UI sem console (pythonw), copiavel para qualquer lugar.

## Decisoes
- FIX-003 (.bat ASCII) e FIX-004 (multi-fonte ao vivo).
- UI por selecao (modal) aprovada; implementada na UI-1.
- Launcher .bat e util e COMPLEMENTAR aos .bat gerados (launcher abre a UI; gerados rodam uma config salva).

## Bugs / problemas
- FIX-003 e FIX-004 corrigidos. Sem bugs abertos.

## Aprendizados / armadilhas
- Ao adicionar opcao que o gerador serializa, ligar a MESMA opcao a execucao ao vivo no mesmo ciclo
  (causa do FIX-004).
- O `cli.py` ja reseta `exclude_ext` na fonte de `.md` (linha 157) -> da para reproduzir QUALQUER allowlist
  do modal so com `--add-ext` + `--exclude-ext`, sem flag nova.
- tkinter: o ganho de UX vem de declutter (modal) + agrupamento, nao de estilo custom.

## Ideias capturadas (dobrar no IDEAS no proximo lote de meta)
- **Launcher da UI:** entregue como `.bat`. Futuro: botao "Gerar atalho da UI" na propria GUI (calcula o
  caminho do run.py sozinho, sem hardcode) — talvez gerar um `.lnk` em vez de `.bat`.
- UI-2 (polimento de layout) e UI-3 (presets "so docs"/"so codigo", lembrar ultima selecao) seguem na fila.

## Onde parei
spec-0005 (fix), spec-0006 (docs) e spec-0007 (UI-1) prontas para aplicar; `flatdrop-ui.bat` entregue.
Falta autorar: `.flatdropignore` + `.gitignore` aninhado; depois `_TREE.md`. O proximo lote de meta
(IDEAS/STATUS) sai junto com a spec do `.flatdropignore`, para as ancoras baterem com o estado ja aplicado.

## Proximos passos
- Usuario aplica spec-0005, spec-0006 e spec-0007 (pytest + smoke da GUI) e commita.
- Autorar `.flatdropignore` (aninhado + negacao `!`) + `.gitignore` aninhado; depois `_TREE.md`; e o lote de meta.
