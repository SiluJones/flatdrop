# Log — 2026-07-28

## Objetivo da sessão
Receber o **template-update do KCM v1.87.0**, comparar cada arquivo genérico com o vivo,
reportar (a) novidades / (b) choques / (c) o que só existe aqui — e, depois da decisão do
autor, aplicar os merges aprovados. Nenhuma mudança de código prevista.

## O que foi feito
- **Comparação completa** dos 18 arquivos `__template-update` contra os vivos (relatório no chat).
- **Merges aplicados** (todos como arquivo inteiro para o autor baixar e substituir):
  - `meta/CEREBRO.md` — princípios 1/8/10/11 ampliados (com destaque para **rever o mount a cada
    turno**), seções novas «Análise antes do compromisso», «Ao receber um template-update do KCM»,
    «Refino das Instruções do Projeto» e «Bloco de fecho de turno»; raias chat↔Code reescritas para
    WO; gatilho de criação de arquivo invertido (não criar arquivo de outro nicho); **apêndice de
    arranque removido** e substituído por tabela apontando os arquivos reais. 297 → 325 linhas.
  - `INSTRUCOES-DO-PROJETO.md` — refinada (6.854 caracteres, sob o teto de ~6.900).
  - `CLAUDE.md` — ganhou «Config (modelo × esforço)», a linha do relatório de trabalho, o
    vocabulário WO/spec e a nota da poda de diretório no mapa rápido. Invariante DEC-020 e mapa
    preservados integralmente.
  - `.claude/skills/apply-wo/SKILL.md` e `.claude/skills/wrap/SKILL.md` — conteúdo vivo (pytest,
    Conventional Commits, portão DEC-020) + front-matter + linha do relatório. Substituem
    `.claude/commands/apply-spec.md` e `wrap.md`.
  - `.gitignore` — união com o lixo de SO/editor. `.flatdropignore` — reescrito na forma `pasta/*`.
  - `meta/README.md` — `HISTORICO` → `HISTORY`, linha do `SPEC.md`, pastas irmãs.
  - `meta/DECISIONS.md` (DEC-023/024/025), `meta/GLOSSARY.md` (5 termos), `meta/IDEAS.md`
    (2 ideias ativas + 4 itens de feedback ao kit), `meta/STATUS.md` (nota de revisão nova,
    backlog reordenado, riscos).
- **Medição no sandbox** (não suposição) do comportamento `pasta/` × `pasta/*` com o mesmo motor
  que o `core._make_spec` usa.

## Decisões
- **DEC-023** — vocabulário: `wo` para o delta aplicável (`meta/workorders/`), `spec` reservado
  para spec de **feature** (SDD). Migração deliberadamente parcial: os 37 arquivos mudam de pasta
  e mantêm o nome; nenhuma referência histórica é reescrita; a próxima é `wo0038`.
- **DEC-024** — comandos do Code viram **Skills**; o apêndice de arranque sai do CEREBRO.
- **DEC-025** — no `.flatdropignore`, pasta se escreve `pasta/*`, nunca `pasta/`.

## Bugs
- **Causa raiz encontrada para o `!` que não funcionava** (notas de 23 e 24/07): não é o motor de
  padrões — `core._scan` **poda o diretório casado antes de descer** (`dirnames[:] = kept`,
  herança do FIX-001), então a negação nunca chega a ser avaliada. Medido:

  | padrões | `meta/legacy/` casa (= poda)? | `GOT.md` ignorado? | `outro.md` ignorado? |
  |---|---|---|---|
  | `meta/legacy/` + `!…/GOT.md` | True | False | True |
  | `meta/legacy/*` + `!…/GOT.md` | False | False | True |

  Ou seja: o motor **já libera** o arquivo; quem o perde é a poda. Contornado por convenção
  (DEC-025); a correção de produto (gerador do editor + `_TREE`) ficou como próxima frente.
- Nenhum bug de código introduzido — não se tocou em `flatdrop/`.

## Aprendizados / armadilhas
- **Poda ≠ filtro.** Ignorar uma pasta e ignorar o conteúdo de uma pasta são coisas diferentes
  quando a varredura poda diretórios: a primeira mata a possibilidade de reincluir. Virou DEC-025
  e entrou no GLOSSARY.
- **Template genérico não é candidato a substituir arquivo vivo refinado.** O atrito se repete a
  cada update do kit; virou seção própria no CEREBRO e crítica no «Feedback para o Kit».
- **Migração de vocabulário é busca-e-troca perigosa:** o que a troca ARRASTA junto (comando,
  caminho, nome de pasta) importa tanto quanto o termo. Registrado no princípio 11 do CEREBRO.

## Onde parei
Merges 1–6 aplicados e entregues como arquivos completos; a mudança física de pasta
(`meta/specs` → `meta/workorders`) e a remoção de `.claude/commands/` estão nos comandos do
fecho, ainda **não executadas**. Merge 7 (análise do multi-raiz) não foi feito — o autor
adiou. Arquivamento do `DECISIONS.md` (793 linhas) adiado de propósito para depois do commit
da migração. Suíte não rodada (nada de código mudou); último número verificado: 68 verdes.

## Próximos passos
- Aplicar os arquivos, rodar os comandos de migração e commitar.
- **Próxima sessão:** os dois bugs de produto — `_TREE` nomear o conteúdo útil das pastas
  ignoradas e o editor da GUI gravar `pasta/*`. Multi-raiz continua adiado.

## Sessão 2 — manhã (12h01): wo0038, FIX-011 e o _TREE nomeando o ignorado

### Objetivo da sessão
Aplicar a wo0038: corrigir o `!` que não resgatava arquivo dentro de pasta ignorada
(causa raiz já medida na sessão anterior) e fazer o `_TREE.md` nomear o que foi
pulado por ignore do autor, em vez de virar só contagem.

### O que foi feito
- **FIX-011 aplicada:** `_negated_dir_prefixes` calcula, dos ignores coletados, as
  pastas alcançadas por alguma negação; a poda de `_scan` só descarta a pasta se ela
  **não** estiver nesse conjunto — conservador com curinga (na dúvida, desce).
- **`_TREE.md` passa a nomear** (até `TREE_NAME_CAP`, depois `(+N mais)`) o arquivo
  pulado por ignore do autor, e pasta ignorada pelo autor ganha uma espiada rasa nos
  filhos diretos (`_peek_children`).
- Versão `0.11.0` → `0.12.0`. 5 testes novos (68 → 73). Commit `74fc806`.

### Decisões
- **FIX-011** registrada em `DECISIONS.md`. Nenhuma DEC nova — a causa raiz já tinha
  virado DEC-025 na sessão anterior.

### Bugs / problemas
- FIX-011: corrigido nesta sessão (ver acima). Nenhum bug novo introduzido.

### Aprendizados / armadilhas
- Poda ≠ filtro se confirma na prática: a correção não mexeu no motor de padrões,
  só em quando a poda decide descartar — reforça o registro já feito na DEC-025.

### Onde parei
wo0038 aplicada, testada e commitada. `_TREE.md` já nomeia o ignorado do autor.

### Próximos passos
- wo0039: merge do template-update do KCM v1.88.0 no CEREBRO.

## Sessão 3 — tarde (12h39–17h26): wo0039 (KCM v1.88.0), wo0040 (DEC-026) e análises do gerador

### Objetivo da sessão
Aplicar o merge cirúrgico do template-update do KCM v1.88.0 (wo0039), resolver a
colisão de logs de sessões passadas e fixar a regra de um log por dia (wo0040), e
redigir as análises do gerador de `.flatdropignore` que embasariam a trava por pasta.

### O que foi feito
- **wo0039:** merge do KCM v1.88.0 no `CEREBRO.md` — regra de higiene (a cópia não é
  fonte da verdade), duas regras a mais no refino das Instruções do Projeto, rodapé
  com as duas versões do kit, comentário explicando por que as Instruções não sobem
  ao mount, e dois itens novos de feedback ao kit sobre a regra `pasta/*`. Commit
  `79696f5`.
- **wo0040:** funde `logs/2026-06-24` e `logs/2026-07-05` (duas versões cada, nada
  descartado) e registra a regra **DEC-026** — um log por DIA, sessões concatenadas
  como seção no mesmo arquivo, propagada no `LOG-TEMPLATE.md` e no `CEREBRO.md`. Duas
  ideias novas no `IDEAS.md` (teto do `_TREE`, `pasta/` vs `pasta/*`); `GLOSSARY.md`
  ganha o termo `pasta/` × `pasta/*`. Commit `8e31ab8`.
- **Análises do gerador de `.flatdropignore`** redigidas e refinadas
  (`meta/analises/260728-ANALISE-gerador-flatdropignore.md` + o modelo
  `meta/analises/_TEMPLATE.md`), preparando o terreno para a trava por pasta da
  wo0041. Commits `1286e19` e `7321267`.

### Decisões
- **DEC-026** (um log por dia, sessões concatenadas) — ver `DECISIONS.md`.

### Bugs / problemas
- Nenhum.

### Aprendizados / armadilhas
- **Logs colididos perdem informação, não se resolvem por sufixo:** os dois arquivos
  de `2026-06-24` e `2026-07-05` diziam "cobre o dia inteiro" e nenhum cobria — virou
  a motivação concreta da DEC-026.

### Onde parei
wo0039 e wo0040 aplicadas e commitadas; análises do gerador prontas. A wo0041 ainda
não estava escrita nem aplicada — ficou para a sessão da noite.

### Próximos passos
- Escrever e aplicar a wo0041 (trava por pasta no gerador do core).

## Sessão 4 — noite (20h10–20h29): wo0041 + wo0042, trava por pasta (DEC-027, 0.13.0)

### Objetivo da sessão
Aplicar a trava por pasta no gerador do `.flatdropignore` (wo0041, core) e no editor
da GUI (wo0042), fechando a DEC-027 e a versão 0.13.0.

### O que foi feito
- **wo0041:** `build_flatdropignore` ganha o parâmetro `locks` (`rel_pasta → bool`); a
  heurística de colapso automático é removida (DEC-027). Trava fechada emite
  `pasta/*`; aberta e escondida pelo git emite `!pasta/*`; arquivos seguem o
  resgate/exclusão por nome conforme a trava mais próxima. Commit `d631230`.
- **wo0042:** coluna nova no editor da GUI, só para pastas, responde "arquivo novo
  aqui entra?" independente do checkbox de presença; `build_flatdropignore` ganha
  `folder_is_closed` como sonda de leitura para a GUI pintar o estado atual ao abrir o
  editor. Fecha a versão **0.13.0**. Commit `598296c`.

### Decisões
- **DEC-027** — a trava da pasta decide o futuro; o checkbox decide o presente
  (altera o contrato da DEC-016). Ver `DECISIONS.md`.

### Bugs / problemas
- Nenhum novo. Feedback registrado ao kit sobre o bloco de fecho de turno confabular
  estado não verificado.

### Aprendizados / armadilhas
- Separar "o que sobe agora" (checkbox) de "o que sobe no futuro" (trava) resolveu uma
  pergunta dupla que um controle só nunca respondia direito — ver DEC-027.

### Onde parei
0.13.0 fechada e commitada (wo0041 + wo0042).

### Próximos passos
- wo0043: trocar o teto simples do `_TREE.md` por uma amostra (primeiros + últimos),
  sessão seguinte (29/07) — ver `logs/2026-07-29.md`.
