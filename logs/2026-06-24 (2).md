# Log — 2026-06-24

> Substitui versoes anteriores deste arquivo (cobre o dia inteiro).

## Objetivo da sessao
Diagnosticar o erro dos `.bat` no CMD; autorar/implementar as features confirmadas (tipos, GUI, gerador
de `.bat`, multi-fonte, modal de tipos UI-1, launcher); e o `.flatdropignore` + `.gitignore` aninhado.

## O que foi feito (resumo do dia)
- **FIX-003:** `.bat` com travessao/acentos + `chcp 65001` desalinha o CMD. 5 `.bat` reentregues em ASCII.
- **spec-0003/0004** (GUI filtros + gerador de `.bat`; docs) APLICADAS (e56aa81).
- **FIX-004 / spec-0005:** multi-fonte agora vale na execucao ao vivo (nao so no `.bat`); Procurar na
  pasta-pai; abrir maximizada. **spec-0006** (docs) APLICADA (cf658b7).
- **UI-1 / spec-0007** APLICADA (f09de95): caixa de extensoes e campos so/exceto trocados por um MODAL
  (checklist categorizado + busca + grupos + custom); tela compacta; `.bat` reproduz a selecao com
  `--add-ext`/`--exclude-ext`; Gerar `.bat` abre na pasta-pai. Launcher `flatdrop-ui.bat` entregue.
- **`.flatdropignore` + `.gitignore` aninhado / spec-0008** (autorada, com 3 testes): ignore proprio,
  aninhado, com negacao `!` para liberar o que o `.gitignore` bloqueia (ate pasta podada). **Logica
  VERIFICADA no sandbox com pathspec real** antes de virar spec. DEC-014.
- **spec-0009** (lote de meta): DEC-014; STATUS limpo (specs aplicados viram resumo); IDEAS (launcher/
  atalho da UI, pratica de verificar no sandbox); ROADMAP marcado.

## Decisoes
- DEC-014: `.flatdropignore` (aninhado, negacao, tem a palavra final sobre o `.gitignore`) + `.gitignore`
  aninhado. Modelo de "ultima regra vence" com rebasing dos padroes de subpasta.
- UI por selecao (UI-1) entregue; UI-2/UI-3 na fila. Launcher e complementar aos `.bat` gerados.

## Bugs / problemas
- FIX-003 e FIX-004 corrigidos. Sem bugs abertos.

## Aprendizados / armadilhas
- Verificar regra sutil no sandbox ANTES de escrever a spec (o `.flatdropignore` pegou uma expectativa
  minha errada e deu confianca no algoritmo).
- `cli.py` ja reseta `exclude_ext` na fonte de `.md` -> da para reproduzir qualquer allowlist do modal
  com `--add-ext` + `--exclude-ext`, sem flag nova.
- `.flatdropignore` sobrepoe o `.gitignore` (decisao deliberada, != git puro) para casar com a intencao.

## Onde parei
spec-0008 (`.flatdropignore`, com testes) e spec-0009 (lote de meta) prontas para aplicar. UI-1 e o
launcher ja entregues e (specs 0005-0007) aplicados. Falta autorar: `_TREE.md`.

## Proximos passos
- Usuario aplica spec-0008 (pytest deve dar 29/29) e spec-0009; commita.
- Autorar `_TREE.md` (pastas ignoradas em uma linha, sem recursao).
- Documentar `.flatdropignore` no README/CONTEXT (proximo lote de doc).
