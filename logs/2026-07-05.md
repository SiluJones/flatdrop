# Log — 2026-07-05

> Cobre o dia inteiro, em tres sessoes. Fusao dos dois arquivos que existiam para esta data
> (`2026-07-05.md` e `2026-07-05 (novo menor, analisar e fazer merge com outro).md`): nao eram
> duplicatas — eram sessoes diferentes do mesmo dia. Fundidos em 2026-07-28, nada descartado.

## Sessao 1 — manha: spec0014 verificada, spec0015 autorada

### Objetivo da sessão
Verificar a aplicação da spec0014 (ajuste de ordem do root_in_name) e autorar a spec
de release para resolver o descompasso de versão (root_in_name no código sem constar
no CHANGELOG da 0.3.0).

### O que foi feito
- Relida a nota nova do mount (`260705-0952.txt`): **spec0014 aplicada** — âncora
  bateu, `_plan_names` agora faz `(*reversed(dir_parts), root_prefix)` sem tocar
  `_compose`; teste ajustado para `page__users__app__proj.tsx` + asserção travando
  "raiz é o último token"; 41 verdes; commit `5acf16f`. Confirmado no core do mount.
- **spec0015 autorada** (`260705-spec0015-release-0.3.1.md`): corte da 0.3.1 —
  bump `0.3.0→0.3.1` no `__init__`, nova seção `[0.3.1]` no CHANGELOG com o
  `root_in_name` (Adicionado) e o fix de ordem (Corrigido), sem reescrever a
  `[0.3.0]` já datada. Deixada a via alternativa 0.4.0 (MINOR) como decisão de
  versionamento do usuário.
- Atualizados STATUS, IDEAS e ROADMAP: 0014 aplicada; 0015 na fila; o "descompasso de
  versão" passou de ATENÇÃO para SOLUÇÃO NA FILA.

### Decisões
- **Versionamento 0.3.1 (patch) vs 0.4.0 (minor):** adotado PATCH na spec (root_in_name
  é adição pequena e opt-in, e a sessão foi majoritariamente acabamento/correção da
  ordem). A spec0015 documenta como trocar para 0.4.0 se o usuário preferir tratar
  como recurso novo. Sem DEC formal — é política de release, registrada na spec.
- Nenhuma DEC/FIX nova no DECISIONS.

### Bugs / problemas
- Nenhum. A spec0014 saiu limpa; a ordem final do root_in_name está verificada no
  core do mount.

### Aprendizados / armadilhas
- **Cortar release cedo cria descompasso:** a 0.3.0 foi datada antes de uma feature
  (root_in_name) entrar, deixando código à frente do CHANGELOG. Lição para o modo
  duas-raias: datar a versão só quando o lote de specs daquele ciclo estiver todo
  aplicado, ou assumir o patch de acerto (o caminho tomado aqui).

### Onde parei
spec0015 autorada e entregue; STATUS/IDEAS/ROADMAP atualizados. Nenhum código de
produto tocado pelo chat.

### Próximos passos
- Aplicar `/apply-spec 260705-spec0015-release-0.3.1.md`; `pytest` (41 verdes,
  sanidade); conferir a ordem das seções do CHANGELOG.
- Commitar com o bloco pronto da spec.
- Fila de produto seguinte (Fase 2): item C (persistir config + pastas recentes) ou o
  trecho de KCM (ler _TREE → gerar .flatdropignore), a critério do usuário.

## Sessao 2 — tarde: FIX-005 (pytest puro)

### O que foi feito
- Relidas as notas novas do mount: **spec0015 aplicada** (nota 1019, commit `fb301f0`,
  0.3.1, 41 verdes) e o **relato de erro do pytest** (nota 1511).
- Diagnosticado o erro do `pytest`: NÃO é regressão da 0.3.1. `pytest` puro na raiz
  falhava a coleta com `ModuleNotFoundError: No module named 'flatdrop'`, enquanto
  `python -m pytest` passava.
- **Causa raiz** (confirmada no mount + reproduzida no sandbox): estrutura aninhada
  (raiz `FlatDrop\flatdrop\`, pacote `flatdrop\` dentro, `tests\` ao lado); nada punha
  a raiz no `sys.path` na coleta; sem `conftest.py`/`pyproject`/`pytest.ini`. O `-m`
  do `python -m pytest` mascarava. Bug latente desde sempre.
- **spec0016 autorada**: `conftest.py` de 3 linhas na raiz (espelha o `run.py`), o
  menor diff que faz `pytest` puro funcionar para sempre. Reproduzido o erro e a cura
  no sandbox antes de escrever.
- Registrado **FIX-005** no DECISIONS; STATUS atualizado (bug em correção + workaround).

### Decisões
- **FIX-005** (novo). Alternativas `pyproject.toml [pythonpath]` e `pip install -e .`
  descartadas por exigirem packaging/passo de ambiente, contra o zero-setup do repo.

### Aprendizados / armadilhas
- **Diferença de ambiente esconde bug:** o Code rodava `python -m pytest` (resolvia o
  path); o usuário roda `pytest` puro (não resolvia). Fixar a forma de rodar na infra
  (`conftest.py` na rootdir), não na memória de quem invoca.
- **Workaround imediato dado ao usuário:** `python -m pytest -q` destrava sem esperar
  a spec ser aplicada.

### Onde parei
spec0016 autorada e entregue; DECISIONS (FIX-005) e STATUS atualizados. Nenhum código
de produto tocado.

### Próximos passos
- Aplicar `/apply-spec 260705-spec0016-conftest-pytest-path.md`; validar com `pytest -q`
  (puro) na raiz → 41 verdes. Commit pronto na spec.
- Alinhar o texto de "como rodar testes" no README para `pytest -q` (ciclo de doc,
  chat) — agora que o puro funciona.
- Fila de produto: item C (persistir config + recentes) ou o trecho de KCM.


## Sessao 3 — noite: fecho da 0.3.1 e transferencia de conversa

### Objetivo da sessão
Fechar o ciclo de release (0.3.1), confirmar que o `pytest` voltou a passar, e
preparar a TRANSFERÊNCIA de conversa: regenerar todos os meta/ completos e detalhados
para a próxima conversa continuar sem perda, já apontada para as duas próximas
tarefas (trecho de KCM + editor de `.flatdropignore` na GUI).

### O que foi feito
- Confirmado no mount: **spec0016 aplicada** (`conftest.py` na raiz existe), versão
  **0.3.1**, `pytest` puro OK. Specs 0011–0016 todas aplicadas e commitadas.
- Respondida a pergunta do usuário ("só sobraram essas duas ideias/fases?"): NÃO —
  mapa completo do aberto entregue (Fase 2: C, D=editor de flatdropignore, multi-raiz,
  UI-2/UI-3; Fase 4: exe, single-file, tokens; stand-by; + o trecho de KCM). As duas
  priorizadas (KCM + editor) são as certas: o editor É o item D, e o KCM é rápido e
  destrava o fluxo do `_TREE.md`.
- **Regenerados TODOS os meta/ completos** (transferência): CONTEXT (reescrito para
  0.3.1 — estava pré-0.3.0), STATUS (limpo, backlog priorizado com as 2 próximas no
  topo), IDEAS (repriorizado — KCM e editor no topo das Ativas), ROADMAP (nova ordem
  da Fase 2), CHANGELOG (FIX-005 no [Não lançado]), GLOSSARY (termos da 0.3.x),
  DECISIONS (já tinha FIX-005 — mantido). LOG-TEMPLATE intacto (molde fixo).
- Escrito o prompt de início da próxima conversa (arquivo `PROMPT-INICIO-KCM.md`).

### Decisões
- Nenhuma DEC/FIX nova nesta sessão (foi transferência + release já cortada).
- **Ordem de trabalho definida:** (1) trecho de KCM, (2) editor de `.flatdropignore`
  na GUI, (3) item C. O editor consolida o antigo item D da Fase 2.

### Bugs / problemas
- Nenhum aberto. FIX-005 (pytest puro) já resolvido pela spec0016.

### Aprendizados / armadilhas
- **O editor de `.flatdropignore` é UI não-trivial:** árvore navegável + estado
  tri-state (incluído/excluído/liberado via `!`) + leitura do que o git já ignora +
  geração dos padrões. Provável spec de investigação/design ANTES da de implementação.
- **CONTEXT tinha ficado para trás** (ainda descrevia 0.2.0/27 testes) enquanto
  STATUS/CHANGELOG avançavam. Lição: em transferência, reler e reescrever o CONTEXT,
  não só os rolantes.

### Onde parei
Todos os meta/ regenerados e entregues para download. Nenhum código tocado. Projeto
em 0.3.1, 41 testes verdes, sem bug aberto. Pronto para nova conversa.

### Próximos passos
- **Nova conversa (tarefa 1):** autorar o trecho de KCM que ensina o Claude a ler o
  `_TREE.md` e ditar um `.flatdropignore` (foco em `!` para liberar o que o git
  esconde) + exemplo no README.
- **Tarefa 2:** editor de `.flatdropignore` na GUI — começar por uma spec de
  investigação/design (estrutura da árvore, estado tri-state, leitura do gitignore,
  geração dos padrões), depois a spec de implementação.
- **Tarefa 3:** item C (persistir config + pastas recentes).
