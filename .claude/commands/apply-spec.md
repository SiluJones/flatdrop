Leia o arquivo de spec indicado em `meta/specs/` e execute-o.
Localize cada âncora EXATAMENTE; se não achar uma, PARE e reporte — não chute um lugar próximo.
Não toque em nada fora das edições nomeadas. Se a spec mexe em CÓDIGO, rode `python -m pytest -q` ao fim.
Ao terminar, rode `git diff` e confira a forma esperada antes de commitar (mensagem SEM acento).
Spec: $ARGUMENTS
